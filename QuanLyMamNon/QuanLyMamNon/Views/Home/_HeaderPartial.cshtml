<header id="header">
    <div class="left-part"></div>
    <a id="logo" href="@Url.Action("Index", "Home")">Kid' school 28/10</a>
    <div class="bar-holder">
        <a class="menu_trigger" href="#">menu</a>
        <nav id="nav">
            <ul>
                <li><a href="@Url.Action("About", "Home")">About us</a></li>
                <li><a href="@Url.Action("Events", "Home")">Events</a></li>
                <li><a href="@Url.Action("Gallery", "Home")">Gallery</a></li>
                <li><a id="modal_trigger" href="#modal">Infomation Student</a></li>
                <li><a href="@Url.Action("Register", "Home")">Register</a></li>
            </ul>
        </nav>
    </div>
    <div id="modal" class="popupContainer" style="display:none;">
        <header class="popupHeader">
            <span class="header_title">Login</span>
            <span class="modal_close"><i class="fa fa-times"></i></span>
        </header>

        <section class="popupBody">
            <!-- Username & Password Login form -->
            <div class="user_login">
                <form action="@Url.Action("Infomation", "Home")" method="post">
                    <label>Email / Username</label>
                    <input type="text" id="id" name="id" />
                    <div class="action_btns">
                        <button class="btn_login_panda" type="submit">Login </button>
                    </div>
                </form>
            </div>
        </section>
    </div>
    @*<div id="modal" class="popupContainer" style="display:none;">
        <div class="panda">
            <div class="ear"></div>
            <div class="face">
                <div class="eye-shade"></div>
                <div class="eye-white">
                    <div class="eye-ball"></div>
                </div>
                <div class="eye-shade rgt"></div>
                <div class="eye-white rgt">
                    <div class="eye-ball"></div>
                </div>
                <div class="nose"></div>
                <div class="mouth"></div>
            </div>
            <div class="body_panda"> </div>
            <div class="foot">
                <div class="finger"></div>
            </div>
            <div class="foot rgt">
                <div class="finger"></div>
            </div>
        </div>
            <form action="Home/Infomation" method="post" class="form_panda">
                <div class="hand"></div>
                <div class="hand rgt"></div>
                <h1>Search...</h1>
                <div class="form-group">
                    @Html.TextBoxFor(model => model.MaHocSinh, new { @required = "required", @class = "form-control", @placeholder = "Nhập mã học sinh..." })
                    <label class="form-label">Username</label>
                </div>
                <div class="form-group">
                    <p class="alert">Invalid Credentials..!!</p>
                    <button class="btn_login_panda" type="submit">Login </button>
                </div>
            </form>

    </div>*@
</header>